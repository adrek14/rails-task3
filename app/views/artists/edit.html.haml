
  
- form_for @artist do |f|
  = render "error_messages", :target => @artist
  = render :partial => "form", :locals => {:f => f}
  %p= f.submit "Zapisz zmiany" 
  %p
  %p= link_to "(x) Usuń", artist_path(@artist), :confirm => "Jestes pewien?", :method => :delete
