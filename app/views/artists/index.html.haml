%h1 Rock 'n Roll Hall of Fame
%p
- if @artists != nil
  %table
    %tr
    - @artists.each do |a|
      %tr
        %td
          %b= link_to a.name, a
          %p{ :style => "display: inline; font-size: 10px;" }= link_to "(edit)", edit_artist_path( a )
          %p{ :style => "display: inline; font-size: 10px;" }= link_to "(usuń)", artist_path( a ), :confirm => "Jestes pewien?", :method => :delete
          %p= a.genre
- else
  %p Brak artystow w bazie!
%p
%p= link_to "(+) Dodaj wykonawcę", new_artist_path
